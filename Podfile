# Podfile

platform :ios, '11.0'

use_frameworks!

def main_pods
  pod 'Alamofire'
  pod 'Plaid'
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'FirebaseFirestoreSwift'
end

target 'Banking' do
  main_pods
end

target 'Sandbox' do
  main_pods
end
